import {Ellipsis,FileSpreadsheet,FileText,Plus} from 'lucide-react'
import {mockFiles} from '../data/mockFiles'
export default function FilePanel({open,onClose,file,setFile}){return <aside className={'files '+(open?'open':'')}><div className="panel-top"><label className="add"><Plus size={17}/>Add Files<input style={{display:'none'}} type="file" accept=".txt,.pdf,.docx" onChange={e=>setFile(e.target.files?.[0]||null)}/></label><button className="close" onClick={onClose}>Close</button></div>{file&&<p className="selected-file">Selected: {file.name}</p>}<div className="file-title"><strong>Your Files <span>(5)</span></strong><button>Sort by: Name ▾</button></div>{mockFiles.map(f=>{let I=f.type==='xls'?FileSpreadsheet:FileText;return <div className="file" key={f.name}><I size={22} className={f.type}/><div><strong>{f.name}</strong><small>{f.meta}</small></div><button aria-label={'More options for '+f.name}><Ellipsis size={18}/></button></div>})}</aside>}
