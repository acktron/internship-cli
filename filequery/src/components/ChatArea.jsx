import {Paperclip,Send} from 'lucide-react'
const prompts=['Summarize','Find mentions of…','List key points']
export default function ChatArea({query,setQuery,file,answer,error,loading,onSubmit,onPickFile}){return <main><section><h1>Ask Anything About Your Files</h1><p className="muted">Query a single local document.</p><div className="prompts">{prompts.map(x=><button key={x} onClick={()=>setQuery(x)}>{x}</button>)}</div>{file&&<p className="active-file">ACTIVE FILE — {file.name}</p>}{loading&&<div className="message loading">PROCESSING DOCUMENT…</div>}{answer&&<div className="message answer"><strong>ANSWER</strong><p>{answer}</p></div>}{error&&<div className="message error" role="alert">{error}</div>}</section><div className="composer"><div><input value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>e.key==='Enter'&&onSubmit()} placeholder="Ask a question about your files..." aria-label="Ask a question"/><button onClick={onPickFile} aria-label="Choose a file"><Paperclip size={17}/></button><button className="send" onClick={onSubmit} aria-label="Send" disabled={loading}><Send size={15}/></button></div><small>ENTER TO RUN QUERY</small></div></main>}
