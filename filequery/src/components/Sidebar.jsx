import {Clock3,FileText,MessageSquare,Settings} from 'lucide-react'
const nav=[['Chat & Search',MessageSquare],['My Files',FileText],['Recent Queries',Clock3],['Settings',Settings]]
export default function Sidebar({mode,setMode,activeNav,setActiveNav}){return <aside className="sidebar"><div className="mode-toggle">{['Local','Online'].map(x=><button key={x} className={mode===x?'selected':''} onClick={()=>setMode(x)}>{x}</button>)}</div><div className="brand"><b className="mark">≡</b><strong>FileQuery</strong></div><nav>{nav.map(([x,I])=><button key={x} className={activeNav===x?'active':''} onClick={()=>setActiveNav(x)}><I size={15}/>{x}</button>)}</nav><div className="side-bottom"><div className="profile"><b>A</b><div><strong>Abhinav Kumar Singh</strong><small>Student</small></div></div></div></aside>}
